import React from 'react'

const PlayerScore = (props) => {
    return (
        <canvas className={"plScore"+props.class} width={200} height={50}>   
        </canvas>
    )
}

export default PlayerScore
